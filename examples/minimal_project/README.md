# Minimal Project Example

This is a synthetic adopter example. It shows the smallest Project Ops configuration shape without carrying real project history.

Files:

- `.project_ops/config.json`

Use this example as a starting point for a new adopter, then replace project identity, scope labels, required docs, privacy rules, and validation policy with local values.
